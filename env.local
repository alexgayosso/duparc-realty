# Copia este archivo como .env.local (mismo folder) y pon tu API Key real.
# .env.local YA está en .gitignore por defecto en Next.js — nunca se sube a GitHub.

EASYBROKER_API_KEY=bhk1acsxu7y95p8l5pcih5m2pl2l50